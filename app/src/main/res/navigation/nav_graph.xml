<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_voice">

    <!-- Fragmento de Voz -->
    <fragment
        android:id="@+id/navigation_voice"
        android:name="com.example.ojovirtual.presentation.ui.VoiceFragment"
        android:label="Asistente de Voz"
        tools:layout="@layout/fragment_voice" />

    <!-- Fragmento de Cámara -->
    <fragment
        android:id="@+id/navigation_camera"
        android:name="com.example.ojovirtual.presentation.ui.CameraFragment"
        android:label="Detección de Objetos"
        tools:layout="@layout/fragment_camera" />

    <!-- ✅ NUEVO: Fragmento de Configuración -->
    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.example.ojovirtual.presentation.ui.SettingsFragment"
        android:label="Configuración de Voz"
        tools:layout="@layout/fragment_settings" />

</navigation>